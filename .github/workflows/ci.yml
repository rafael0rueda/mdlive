name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Tests (Neovim ${{ matrix.neovim }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        # Oldest supported release, current stable and nightly.
        neovim: [v0.11.0, stable, nightly]
    steps:
      - uses: actions/checkout@v7

      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.neovim }}

      - name: Smoke test
        run: make test

      - name: Help tags
        run: make helptags

  browser:
    name: Browser tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      # The runner image comes with Google Chrome.
      - name: Browser tests
        run: make browser

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      # The Neovim runtime provides the types of the vim and vim.uv modules.
      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable

      # The versions pinned in the Makefile.
      - name: Install stylua and lua-language-server
        run: |
          make tools TOOLS="$RUNNER_TEMP/tools"
          echo "$RUNNER_TEMP/tools/bin" >> "$GITHUB_PATH"

      - name: lua-language-server --check
        run: make typecheck

  style:
    name: Stylua
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # The versions pinned in the Makefile.
      - name: Install stylua and lua-language-server
        run: |
          make tools TOOLS="$RUNNER_TEMP/tools"
          echo "$RUNNER_TEMP/tools/bin" >> "$GITHUB_PATH"

      - name: stylua --check
        run: make lint
